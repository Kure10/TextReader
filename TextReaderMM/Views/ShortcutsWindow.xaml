<Window x:Class="TextReaderMM.Views.ShortcutsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Keyboard shortcuts"
        Width="600" Height="730"
        WindowStartupLocation="CenterOwner"
        Background="White">
    <DockPanel Margin="12">
        <Button DockPanel.Dock="Bottom"
                Content="Close"
                Padding="18,3"
                Margin="0,12,0,0"
                HorizontalAlignment="Right"
                IsCancel="True"
                IsDefault="True" />

        <ListBox x:Name="ShortcutList" BorderThickness="1">
            <ListBox.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"
                                       FontWeight="Bold"
                                       Margin="0,10,0,4" />
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListBox.GroupStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Gesture}"
                                   Width="200"
                                   Margin="0,0,50,0"
                                   FontFamily="Consolas" />
                        <TextBlock Text="{Binding Description}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</Window>
